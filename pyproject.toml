[project]
name = "kettlepack-i"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = []

[project.scripts]
build = "kettlepack.main:main"

[tool.hatch.build.targets.wheel]
packages = ["src/kettlepack"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
